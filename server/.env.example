
# --- Pump.fun / streaming ---
RTMP_URL=rtmp://example-ingest/pumpfun
STREAM_KEY=your_stream_key_here

# --- Server ---
PORT=4000

# --- Personality flags ---
DEVICE=sidekick

# --- Streamer Mode ---
# STREAM_MODE=obs   # preferred: uses OBS via obs-websocket
# STREAM_MODE=ffmpeg # test bars; replace with screen/browser capture for real overlay

# --- OBS ---
OBS_WS_URL=ws://127.0.0.1:4455
OBS_WS_PASSWORD=

# --- Pump.fun Chat (adapter stub) ---
PUMPFUN_WS_URL=
PUMPFUN_TOKEN_PAGE=
PUMPFUN_HEADLESS=0
